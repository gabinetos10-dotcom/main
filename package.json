{
  "name": "prospection-sans-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Collecte d'enseignes locales sans vrai site web depuis Google Places, enrichissement des adresses e-mail et export vers la console de prospection.",
  "bin": {
    "prospect": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "demo": "node src/cli.mjs demo --out data/demo.json && node src/cli.mjs export --in data/demo.json --csv data/demo.csv"
  },
  "engines": {
    "node": ">=18.17"
  },
  "license": "MIT"
}
