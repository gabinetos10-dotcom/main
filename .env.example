# Copy to .env before running the app.
# SQLite for local demo — for production switch prisma/schema.prisma
# provider to "postgresql" and point DATABASE_URL at your database.
DATABASE_URL="file:./dev.db"

# Password protecting the /admin dashboard.
ADMIN_PASSWORD="locnjoy-admin"
